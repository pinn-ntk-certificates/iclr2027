[project]
name = "pinn-ntk-experiments"
version = "0.1.0"
description = "Reproducible NTK-regime experiments for the ICLR 2027 PINN convergence paper"
requires-python = ">=3.11"
dependencies = [
  "matplotlib>=3.10",
  "numpy>=2.2",
  "pandas>=2.2",
  "torch>=2.7",
]

[tool.uv]
package = false
